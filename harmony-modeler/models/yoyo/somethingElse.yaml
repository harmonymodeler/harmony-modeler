# yaml-language-server: $schema=https://json-schema.org/draft-07/schema

title: SomeThingElse
type: object
description: Schemas repository holding data schemas
additionalProperties: false
properties:
  id:
    type: string
    description: Internal UUID if the repository
    format: uuid
  createdAt:
    type: string
    description: The date at which the repository was created in the db
    format: date-time
  organization:
    type: string
    description: Organization owner of the repository
  name:
    type: string
    description: The name of the repository
required:
  - id
  - createdAt
  - organization
  - name
